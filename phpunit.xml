<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
         failOnWarning="true"
         failOnRisky="true"
         failOnDeprecation="true"
         beStrictAboutOutputDuringTests="true"
>
    <testsuites>
        <testsuite name="Arch">
            <directory>tests/Arch</directory>
        </testsuite>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
        <exclude>
            <directory>app/Console/Commands</directory>
        </exclude>
    </source>
    <php>
        <!--
            The architecture tests parse every file under app/ in one process,
            and the default 128M ran out as the codebase grew: the symptom is a
            fatal error inside php-parser on whichever file happened to be
            next, which reads as a broken class rather than as a limit. The
            product itself is not held to this number - a request parses
            nothing.
        -->
        <ini name="memory_limit" value="1G"/>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="BROADCAST_CONNECTION" value="null"/>
        <env name="CACHE_STORE" value="array"/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="ACCESS_CACHE_TTL" value="0"/>
        <env name="LOG_STRUCTURED" value="false"/>
        <!--
            Tests run against MariaDB, not SQLite. Collation, JSON functions
            and foreign-key behaviour differ enough that a green SQLite suite
            would not tell us the product works.
        -->
        <env name="DB_CONNECTION" value="mariadb"/>
        <env name="DB_DATABASE" value="infracms_test"/>
    </php>
</phpunit>
